/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Allow phone / LAN access during `next dev` (HMR cross-origin). Add your Mac LAN IP if it changes. */
  allowedDevOrigins: ["192.168.1.100"],
};

export default nextConfig;
